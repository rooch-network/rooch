# Grow Bitcoin

## 简介

![](/docs/grow-bitcoin/grow-bitcoin-home.png)

Grow Bitcoin 是一个生态系统加速平台，专注于将比特币用户和利益相关者与项目连接起来。Grow Bitcoin 由 Rooch Network 团队运营。

作为比特币质押者或用户，您可以通过为您喜欢的项目投票来赚取 BitXP 和项目 XP，以您的比特币作为质押，但根本无需移动您的比特币。

作为比特币项目建设者，您可以自由地在 Grow Bitcoin 平台上列出您的项目，并获得潜在比特币用户的关注；用户将用他们的选票支持您的项目，并且选票越多，您也可以获得 BitXP！

- Grow Bitcoin 网址：https://grow.rooch.network/

> 测试网：https://test-grow.rooch.network

## FAQ

### 主网常见问题

#### Self Staking 风险

Self Staking 对 Bitcoin 来说只有 `Read` 权限，没有 `Transfer`，保证了 BTC 的所有权。在 Rooch 层具有 `Write` 权限，提供了扩展性。

因此 Grow Bitcoin 中的 Self Staking 是零风险的质押挖矿。

举个例子，我要去一个新开的超市买东西，我想办一张会员卡，会员卡只会读取我的身份信息，无法再做更多的操作。Grow Bitcoin 的质押挖矿就是这种模式，只能读取 Bitcoin 钱包的 UTXO，没有权限去操作您的 Bitcoin 资产。

#### 为什么要创建 Session Key

Rooch 的 Session Key 为应用交互提供了安全、简化 DApp 交易流程。

Session Key 的详细描述请参考这篇文档 https://rooch.network/zh-CN/learn/core-concepts/accounts/session-key

#### 详细使用流程

请参考 [Grow Bitcion 测试网使用指南](https://rooch.network/zh-CN/blog/grow-bitcoin-test)。

> 提示：主网与测试网使用流程一致，注意访问网址的 URL！

#### 如何获取 UTXO 和 RGas

只需要确保 UniSat 钱包有 UTXO，并在这里 [Claim RGas](https://portal.rooch.network/faucet) 即可质押。

RGas 是 Rooch 网络上的原生 gas 代币，用于支付交易和运营成本。

#### 账户有 BTC 了但是没有 UTXO

BTC 转账交易，需要等待三个区块确认后，`UTXO` 才可以在 Rooch 中使用。

#### Grow 怎么挖矿

只需要自己的钱包持有 BTC 即可质押挖矿，获取 `GROW`。Grow Bitcoin 采取的是 `Hold Mining` 的质押模式来获取 `GROW`。

注意：如果自己钱包的 UTXO 花费出去后，在 Grow Bitcoin 质押的 UTXO 就失效了，需要手动质押新的 UTXO 继续挖矿。

#### 循环出现创建 Seesion

如果循环弹出 `Create Session` 的提示窗，请检查是否有 RGas。

#### 质押是否影响 ARC20

不影响。质押不需要发送 L1 交易。

#### 需要多少 BTC 可以质押

无准入门槛。

#### Grow 总量

GROW 总量为 `210,000,000,000`，`180` 天挖完。

每天挖矿数量固定，按质押的 BTC 分成。

> 源码：https://github.com/rooch-network/rooch/blob/main/apps/grow_bitcoin/sources/grow_bitcoin.move

#### 有质押限制吗

没有。

#### RGas 没有了

如果没有 RGas 了，可以在[这里](https://portal.rooch.network/gas-swap)购买。

> 提示：目前只提供了 BTC -> RGas，暂时无法 RGas -> BTC，后续将提供 USTD 购买 RGas 功能。

#### UTXO 改变了，GROW 奖励还有吗

因为是持有挖矿模式，如果 UTXO 被花费了，没有领取的 `GROW` 就无法 Claim 了，建议花费 BTC 之前先领取质押奖励。

#### 质押获得的 GROW 可以每天可以 Claim 多少次

没有次数限制，只要有质押收益就可以 Claim。

#### 每次 Claim 消耗多少 RGas

大约 0.02 ~ 0.03 RGas。

#### Babylon Stake

同一个 UTXO 不能 Self Staking 然后再到 Babylon 上 Stake。

需要先在 Babylon stake，然后通过 Babylon Stake Seal 来 Stake。

Babylon 的质押带有时间锁，它的质押具有时间权重。

#### Rooch 有价格预言机了吗

目前有 Bitcoin 的预言机。

> 源码：https://github.com/rooch-network/rooch/blob/main/apps/gas_market/sources/trusted_oracle.move

#### Grow 是 Rooch 的主网代币吗

不是。

#### GROW 有什么作用

可以使用 `GROW` 给项目投票，获取 `BitXP` 积分。`BitXP` 用于获取空投。

#### GROW 投票后会消失吗

投票后 GROW 会被烧掉，减少供应量。

#### GROW 与 BitXP 比率

`GROW` 与 `BitXP` 为 `1:1` 映射，投票消耗的 `GROW` 将获得等量的 `BitXP`。

`GROW` 是完全公平发射的，供应量固定，`BitXP` 没有上限，`BitXP` 只能通过使用 `GROW` 给项目投票获取。

#### 新的 Bitcoin 地址可以领 RGas 吗

新的 Bitcoin 地址可以 `Claim` 一次 RGas。

#### 使用哪种类型的 Bitcoin 地址

Taproot。

#### GROW 在哪里交易

后续将推出 GROW 的交易市场。

### 测试网常见问题

#### 如何获取 UTXO 和 RGas

测试网请使用下面的方式获取 UTXO 和 RGas：

- [获取 tBTC](https://rooch.network/zh-CN/build/getting-started/get-test-btc)
- [获取 Rooch Gas Coin](https://rooch.network/zh-CN/build/getting-started/get-gas-coin)

#### 测试网领取过 BTC 但是没有 UTXO

出现这个问题，请给自己的账户转账一次。

#### Discord 找不到 Faucet 频道

请在 `contributor-role` 频道获取 `seed` 角色。

#### 账户有 BTC 了但是没有 UTXO

BTC 转账交易，需要等待三个区块确认后，`UTXO` 才可以在 Rooch 中使用。

#### Grow Bitcion 使用的是哪个版本的 Bitcoin 测试网

Grow Bitcion 测试网使用的是 Bitcoin `test3` 的测试网。
