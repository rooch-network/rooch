// Copyright (c) RoochNetwork
// SPDX-License-Identifier: Apache-2.0

import { beforeAll, describe, expect, it } from 'vitest'
import { setup, TestBox } from '../setup'
import { Args, bcs } from '@/bcs'
import { Transaction } from '@/transactions'
import { fromHEX, toHEX } from '@/utils'
import { BitcoinAddress } from '@/address'

describe('Checkpoints Error', () => {
  // let testBox: TestBox;
  //
  // beforeAll(async () => {
  //   testBox = await setup();
  // });
  //
  // it('aa', async () => {
  //   const resultSN = await testBox.client.signAndExecuteTransaction();
  //
  //   expect(resultSN).eq(expectSN)
  // });
})
